spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://config-center.com:3344
      name: eureka-server01
      label: master
      fail-fast: true

server:
  port: 7001